# The frontend no longer needs any Google OAuth configuration — the backend
# (see server/.env.example) owns the client ID, client secret, and all token
# handling. This file is intentionally near-empty; see plan/plan.md M3.
